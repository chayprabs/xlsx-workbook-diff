{
  "name": "xlsx-workbook-diff",
  "version": "1.0.0",
  "private": true,
  "description": "Compare Excel XLSX workbooks online at cell, formula, style, chart, named-range and sheet-structure level with tolerance settings.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test && cd apps/worker && python3 -m pytest tests/ -q",
    "dev": "pnpm --parallel -r dev"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0"
}
