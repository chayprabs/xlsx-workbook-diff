export function SeoBar() {
  return (
    <div className="w-full bg-white border-b border-[var(--border)]">
      <div className="max-w-6xl mx-auto px-4 py-3 text-center text-sm text-[var(--muted)] leading-relaxed">
        Compare two Excel <strong className="text-[var(--foreground)] font-medium">.xlsx</strong> workbooks
        online — cell values, formulas, styles, charts, and sheet structure — with numeric tolerance presets
        and downloadable color-coded diff reports.
      </div>
    </div>
  );
}
